```text
curl -X POST https://api.portone.com/v2/platform/transfer/order \
     -u {api_key} \
     -d 'partnerId=lecturer_1' \
     -d 'payment[source] = PORTONE' \
     -d 'payment[paymentId]=merchant_uid1' \
     -d 'discounts=[{"amount":1000, "discountShareId": "discount_1"}]'
```

