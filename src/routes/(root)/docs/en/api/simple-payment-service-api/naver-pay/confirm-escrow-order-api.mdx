---
title: Confirm escrow order API
description: ''
---

import Details from "~/components/gitbook/Details";
import Swagger from "~/components/gitbook/swagger/Swagger";
import SwaggerDescription from "~/components/gitbook/swagger/SwaggerDescription";
import SwaggerParameter from "~/components/gitbook/swagger/SwaggerParameter";
import SwaggerResponse from "~/components/gitbook/swagger/SwaggerResponse";
import Tabs from "~/components/gitbook/Tabs";
import Hint from "~/components/Hint";

## Confirms a Naver Pay escrow order.

<Swagger method="post" path="/payments/{imp_uid}/naver/confirm" baseUrl="https://api.iamport.kr" summary="Confirms a Naver Pay escrow order.">
  <SwaggerDescription>
    Naver Pay - Pay method at checkout
  </SwaggerDescription>

  ### Parameters

  #### Path

  <SwaggerParameter name="imp_uid" type="String" required="true">
    <mark style="color:red;">
      **i'mport transaction ID**
    </mark>
  </SwaggerParameter>

  ### Responses

  <SwaggerResponse status="200: OK" description="Success">
    <Tabs>
      <Tabs.Tab title="ResponseAnnotation">
        **`code`** <mark style="color:red;">**\***</mark> <mark style="color:purple;">**integer**</mark>

        **Response code**

        0: success, Not 0: check the message

        **`message`** <mark style="color:red;">**\***</mark> <mark style="color:green;">**string**</mark>

        **Response message**

        A non-zero code includes a message like 'Invalid payment info'.
      </Tabs.Tab>
    </Tabs>
  </SwaggerResponse>

  <SwaggerResponse status="401: Unauthorized" description="Missing or invalid access token">
    ```ts
    {
      // Response
    }
    ```
  </SwaggerResponse>

  <SwaggerResponse status="404: Not Found" description="Invalid imp_uid or no access permission">
    ```ts
    {
      // Response
    }
    ```
  </SwaggerResponse>

  <SwaggerResponse status="500: Internal Server Error" description="Request failed. Check the response body message.">
    ```ts
    {
      // Response
    }
    ```
  </SwaggerResponse>
</Swagger>

<Details>
  <Details.Summary>Response Model Schema</Details.Summary>

  <Details.Content>
    ```json
    {
      "code": 0,
      "message": "string"
    }
    ```
  </Details.Content>
</Details>

<Hint style="success">
  **Swagger Test Link**

  [**https://api.iamport.kr/#!/naver/naverConfirmPayment**](https://api.iamport.kr/#!/naver/naverConfirmPayment)
</Hint>
