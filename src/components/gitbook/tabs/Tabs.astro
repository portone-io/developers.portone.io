---
import { Tabs as TabsImpl, processTabs } from "./index.solid";

const slotHtml = await Astro.slots.render("default");
const tabs = processTabs(slotHtml);
---

<TabsImpl client:visible uniqueId={crypto.randomUUID()} getTabs={() => tabs} />
