---
import { MethodBadge } from "./index.solid";
import Details from "../Details.astro";

interface Props {
  method: "get" | "post" | "put" | "delete";
  path: string;
  baseUrl: string;
  summary: string;
}
const { method, path, baseUrl, summary } = Astro.props;
---

<Details>
  <div slot="summary">
    <div>
      <MethodBadge method={method} />
      <span class="text-gray ml-2 text-sm"
        ><span>{baseUrl}</span><span class="font-bold text-black">{path}</span
        ></span
      >
    </div>
    <div class="mt-3 font-bold">
      <p>{summary}</p>
    </div>
  </div>
  <slot />
</Details>
