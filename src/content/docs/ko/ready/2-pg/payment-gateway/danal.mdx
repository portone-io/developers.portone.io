---
title: 다날 설정
description: 다날 PG사 설정 방법을 안내합니다.
---

import * as prose from "~/components/prose";
export const components = prose;

import Figure from "~/components/gitbook/Figure.astro";
import Hint from "~/components/gitbook/Hint.astro";
import Tabs from "~/components/gitbook/tabs/Tabs.astro";
import Tab from "~/components/gitbook/tabs/Tab.astro";

import image1 from "./_assets/danal/danal-1.png";
import image2 from "./_assets/danal/danal-2.png";
import image3 from "./_assets/danal/danal-3.png";
import image4 from "./_assets/danal/danal-4.png";
import image5 from "./_assets/danal/danal-5.png";
import image6 from "./_assets/danal/danal-6.png";
import image7 from "./_assets/danal/danal-7.png";
import image8 from "./_assets/danal/danal-8.png";
import image9 from "./_assets/danal/danal-9.png";
import image10 from "./_assets/danal/danal-10.png";

## 인증**결제**

<Tabs>
<Tab title="테스트 결제">
**테스트 환경 구성방법**

1. [포트원 관리자 콘솔](https://admin.portone.io/) 로그인
2. **결제 연동** 탭 클릭
3. `결제대행사 설정 및 추가` 폼에서 테스트 → `다날` → `다날` 선택 후 `+ 추가` 버튼 클릭
4. 자동으로 기재되는 상점정보 확인 후 저장

<Figure src={image1} />

<Figure src={image2} caption="자동으로 기재되는 CPID 정보 확인 후 저장" />

</Tab>

<Tab title="실 결제">
**실 환경 구성방법**

1. [포트원 관리자 콘솔](https://admin.portone.io/) 로그인
2. **결제 연동** 탭 클릭
3. `결제대행사 설정 및 추가` 폼에서 실연동 → `다날` → `다날` 선택 후 `+ 추가` 버튼 클릭
4. 카드사 심사 완료 후 다날에서 발급받은 실상점 정보 기재 후 저장

<Figure src={image3} />

<Figure src={image4} />

</Tab>
</Tabs>

## 비 인증 결제

<Tabs>
<Tab title="결제창 방식">
**테스트 환경 구성방법**

1. [포트원 관리자 콘솔](https://admin.portone.io/) 로그인
2. **결제 연동** 탭 클릭
3. `결제대행사 설정 및 추가` 폼에서 테스트 → `다날` → `다날` 선택 후 `+ 추가` 버튼 클릭
4. 자동으로 기재되는 상점정보 확인 후 저장

<Figure src={image1} />

<Figure src={image2} caption="자동으로 기재되는 CPID 정보 확인 후 저장" />

**실 환경 구성방법**

1. [포트원 관리자 콘솔](https://admin.portone.io/) 로그인
2. **결제 연동** 탭 클릭
3. `결제대행사 설정 및 추가` 폼에서 실연동 → `다날` → `다날` 선택 후 `+ 추가` 버튼 클릭
4. 카드사 심사 완료 후 다날에서 발급받은 실상점 정보 기재 후 저장

<Figure src={image3} />

<Figure src={image4} />

</Tab>

<Tab title="API 방식">
**다날은 **<mark style="color:red;">**API**</mark>** 를 통한 비인증 결제를 지원하지 않습니다.** :cry:

</Tab>
</Tabs>

## 휴대폰 소액결제

<Tabs>
<Tab title="테스트 환경">
**테스트 환경 구성방법**

1. [포트원 관리자 콘솔](https://admin.portone.io/) 로그인
2. **결제 연동** 탭 클릭
3. `결제대행사 설정 및 추가` 폼에서 테스트 → `다날` → `다날 휴대폰결제/본인인증` 선택 후 `+ 추가` 버튼 클릭
4. 자동으로 기재되는 상점정보 확인 후 저장

<Hint style="info">
**다날 휴대폰결제 테스트용 CPID : A010002002**

</Hint>

<Figure src={image5} />

<Figure src={image6} />

</Tab>

<Tab title="실 환경">
**실 환경 구성방법**

1. [포트원 관리자 콘솔](https://admin.portone.io/) 로그인
2. **결제 연동** 탭 클릭
3. `결제대행사 설정 및 추가` 폼에서 실연동 → `다날` → `다날 휴대폰결제/본인인증` 선택 후 `+ 추가` 버튼 클릭
4. 카드사 심사 완료 후 다날에서 발급받은 실상점 정보 기재 후 저장

<Figure src={image7} />

<Figure src={image8} />

</Tab>
</Tabs>

## SMS 본인인증

<Tabs>
<Tab title="테스트 환경">
**테스트 환경 구성방법**

1. [포트원 관리자 콘솔](https://admin.portone.io/) 로그인
2. **결제 연동** 탭 클릭
3. `결제대행사 설정 및 추가` 폼에서 테스트 → `다날` → `다날 휴대폰결제/본인인증` 선택
4. 채널속성 본인인증 선택 후 `+ 추가` 버튼 클릭
5. 카드사 심사 완료 후 다날에서 발급받은 실상점 정보 기재 후 저장

<Hint style="warning">
**다날 SMS 본인인증**

휴대폰 SMS 본인인증은 테스트는 별도의 테스트 전용 CPID 정보를 제공하지 않습니다.

테스트 진행을 위해서는 다날 계약이후 발급받은 정보로만 가능합니다.

</Hint>

<Figure src={image5} />

<Figure src={image9} />

</Tab>

<Tab title="실 환경">
**실 환경 구성방법**

1. [포트원 관리자 콘솔](https://admin.portone.io/) 로그인
2. **결제 연동** 탭 클릭
3. `결제대행사 설정 및 추가` 폼에서 실연동 → `다날` → `다날 휴대폰결제/본인인증` 선택
4. 채널속성 본인인증 선택 후 `+ 추가` 버튼 클릭
5. 카드사 심사 완료 후 다날에서 발급받은 실상점 정보 기재 후 저장

<Figure src={image5} />

<Figure src={image10} />

</Tab>
</Tabs>

<Hint style="warning">
**특이사항**

- 다날은 하나의 상점ID(CPID)에 인증/비 인증 결제 셋팅이 가능합니다.
- 테스트 모드의 경우 실제로 결제승인이 이루어지지만, PG사에서 일괄적으로 매일 자정이전 자동 취소가 이루어집니다.
- 다날 정책상 계좌이체/가상계좌는 테스트 결제가 제공되지 않습니다. 계약 후 실제 상점아이디로 테스트를 해야 합니다
- SMS 본인인증은 통신사 정책으로 인하여 테스트 모드 이용이 불가합니다. (실 계약 후 이용가능)
- 테스트 모드에서 부분취소 테스트는 불가합니다.

</Hint>
