---
title: 하위상점 정보 수정 API
description: 대표상점에 대한 하위상점의 정보를 수정합니다.
---

import Details from "~/components/gitbook/Details.astro";
import Hint from "~/components/Hint.astro";
import Swagger from "~/components/gitbook/swagger/Swagger.astro";
import SwaggerDescription from "~/components/gitbook/swagger/SwaggerDescription.astro";
import SwaggerParameter from "~/components/gitbook/swagger/SwaggerParameter.astro";
import SwaggerResponse from "~/components/gitbook/swagger/SwaggerResponse.astro";
import Tabs from "~/components/gitbook/tabs/Tabs.astro";
import Tab from "~/components/gitbook/tabs/Tab.astro";

### 대표상점에 대한 하위상점의 정보를 수정합니다.

<Swagger method="delete" path="/tiers/{tier_code}" baseUrl="https://api.iamport.kr" summary="하위상점 정보를 수정">
<SwaggerDescription>
하위상점(Tier)정보를 수정합니다.

</SwaggerDescription>

### Parameters

#### Path

<SwaggerParameter name="tier_code" type="String" required="true">
<mark style="color:red;">

**하위상점(Tier)고유코드**

</mark>

**3자리의 알파벳 대문자 또는 숫자만 사용**

</SwaggerParameter>

<SwaggerParameter name="tier_name" type="String" required="true">
<mark style="color:red;">

**하위상점 명칭**

</mark>

(내부 관리용)

</SwaggerParameter>

### Responses

<SwaggerResponse status="200: OK" description="성공">
<Tabs>
<Tab title="TierResponse">
**`code`** <mark style="color:red;">**\***</mark> <mark style="color:purple;">**integer**</mark>

**응답코드**

0이면 정상적인 조회, 0 이 아닌 값이면 message를 확인해봐야 합니다

**`message`** <mark style="color:red;">**\***</mark> <mark style="color:green;">**string**</mark>

**응답메세지**

code 값이 0이 아닐 때, '존재하지 않는 결제정보입니다'와 같은 오류 메세지를 포함합니다

**`response`** <mark style="color:red;">**(TierAnnotation, optional)**</mark>

</Tab>
</Tabs>

<Tabs>
<Tab title="TierAnnotation">
**`code`** <mark style="color:red;">**\***</mark> <mark style="color:purple;">**integer**</mark>

**응답코드**

0이면 정상적인 조회, 0 이 아닌 값이면 message를 확인해봐야 합니다

**`message`** <mark style="color:red;">**\***</mark> <mark style="color:green;">**string**</mark>

**응답메세지**

code 값이 0이 아닐 때, '존재하지 않는 결제정보입니다'와 같은 오류 메세지를 포함합니다

**`tier_code`** <mark style="color:red;">**\***</mark> <mark style="color:green;">**string**</mark>

**하위상점(Tier)의 고유코드**

알파벳 대문자 또는 숫자 3자리

**`tier_name`** <mark style="color:red;">**\***</mark> <mark style="color:green;">**string**</mark>

**하위상점(Tier) 관리명칭**

</Tab>
</Tabs>

</SwaggerResponse>

<SwaggerResponse status="400: Bad Request" description="imp_uid가 누락된 경우/ 가상계좌 결제 건이 아닌 경우 / 가상계좌가 입금대기 상태(ready)가 아닌 경우">
```ts
{
    // Response
}
```

</SwaggerResponse>

<SwaggerResponse status="401: Unauthorized" description="인증 Token이 전달되지 않았거나 유효하지 않은 경우">
```ts
{
    // Response
}
```

</SwaggerResponse>

<SwaggerResponse status="500: Internal Server Error" description="수정에 실패한 경우">
```ts
{
    // Response
}
```

</SwaggerResponse>
</Swagger>

<Details>
<p slot="summary">Response Model Schema</p>

```json
{
  "code": 0,
  "message": "string",
  "response": {
    "tier_code": "string",
    "tier_name": "string"
  }
}
```

</Details>

<Hint style="success">

**Swagger Test Link**

[**https://api.iamport.kr/agency#!/tiers/UpdateTier**](https://api.iamport.kr/agency#!/tiers/UpdateTier)

</Hint>
