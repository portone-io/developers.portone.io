---
title: PG 결제창 이용하기
description: ''
---

import * as prose from "~/components/prose";
export const components = prose;

import ContentRef from "~/components/gitbook/ContentRef.astro";


## **결제창을 통한 빌링키 발급의 장점**

카드정보가 서버 또는 포트원의 서버를 거치지 않고 직접 PG사로 전달되기 때문에 데이터 및 통신구간 암호화 등의 **추가 보안 프로세스를 구현할 필요가 없습니다.**

## 결제창을 통한 빌링키 발급의 단점

PG사의 일반결제창을 통해 카드정보를 입력받기 때문에 웹브라우저를 통해서만 빌링키 발급이 이루어지며, **카드정보 입력란을 커스터마이징 할 수 없습니다.**

## 결제창을 통한 빌링키 발급 구현하기

1. 빌링키 결제 API를 서버에서 구현하여 빌링키를 저장하거나 빌링키를 통해 결제하는 API를 생성합니다.
2. 결제 페이지에서 포트원 SDK를 통해 빌링키를 발급하고, 구현한 빌링키 결제 API에 전달합니다.

자세한 구현 방법은 아래 포스트를 참고하세요.

<ContentRef slug="/ko/1" />

<ContentRef slug="/ko/2" />
