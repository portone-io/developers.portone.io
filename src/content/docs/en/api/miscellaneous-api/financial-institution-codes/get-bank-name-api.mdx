---
title: Get bank name API
description: ''
---

import Details from "~/components/gitbook/Details";
import Swagger from "~/components/gitbook/swagger/Swagger";
import SwaggerDescription from "~/components/gitbook/swagger/SwaggerDescription";
import SwaggerParameter from "~/components/gitbook/swagger/SwaggerParameter";
import SwaggerResponse from "~/components/gitbook/swagger/SwaggerResponse";
import Tabs from "~/components/gitbook/Tabs";
import Hint from "~/components/Hint";

## Gets the bank name for a bank code.

<Swagger method="get" path="/banks/{bank_standard_code}" baseUrl="https://api.iamport.kr" summary="Gets the bank name for a bank code.">
  <SwaggerDescription />

  ### Parameters

  #### Path

  <SwaggerParameter name="bank_standard_code" type="String" required="true">
    <mark style="color:red;">
      **Bank code**
    </mark>
  </SwaggerParameter>

  ### Responses

  <SwaggerResponse status="200: OK" description="Success">
    <Tabs>
      <Tabs.Tab title="StandardCodeListResponse">
        **`code`** <mark style="color:red;">**\***</mark> <mark style="color:purple;">**integer**</mark>

        **Response code**

        0: success, Not 0: check the message

        **`message`** <mark style="color:red;">**\***</mark> <mark style="color:green;">**string**</mark>

        **Response message**

        A non-zero code includes a message like 'Invalid payment info'.

        **`response`**<mark style="color:red;">**`(Array[StandardCodeAnnotation], optional)`**</mark>
      </Tabs.Tab>
    </Tabs>

    <Tabs>
      <Tabs.Tab title="StandardCodeAnnotation">
        **`code`**<mark style="color:red;">**`*`**</mark><mark style="color:green;">**`string`**</mark>

        **`Institution code (from KFTC)`**

        **`name`` `**<mark style="color:red;">**`*`**</mark><mark style="color:green;">**`string`**</mark>

        **`Institution name (from KFTC)`**
      </Tabs.Tab>
    </Tabs>
  </SwaggerResponse>

  <SwaggerResponse status="401: Unauthorized" description="Missing or invalid access token">
    ```ts
    {
      // Response
    }
    ```
  </SwaggerResponse>

  <SwaggerResponse status="404: Not Found" description="Result not found">
    ```ts
    {
      // Response
    }
    ```
  </SwaggerResponse>
</Swagger>

<Details>
  <Details.Summary>Response Model Schema</Details.Summary>

  <Details.Content>
    ```json
    {
      "code": 0,
      "message": "string",
      "response": {
        "code": "string",
        "name": "string"
      }
    }
    ```
  </Details.Content>
</Details>

<Hint style="success">
  **Swagger Test Link**

  [**https://api.iamport.kr/#!/codes/bankCodes**](https://api.iamport.kr/#!/codes/bankCodes)
</Hint>
