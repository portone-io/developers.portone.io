---
import { useSystemVersion } from "#state/system-version";

const systemVersion = useSystemVersion();

return new Response(null, {
  status: 301,
  headers: {
    Location: `/api/rest-${systemVersion}`,
  },
});
---
